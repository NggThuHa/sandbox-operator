---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: virtualinstances.lab.ngtukien.id.vn
spec:
  group: lab.ngtukien.id.vn
  names:
    kind: VirtualInstance
    listKind: VirtualInstanceList
    plural: virtualinstances
    singular: virtualinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The phase of the VirtualInstance
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: The VirtualCluster this instance belongs to
      jsonPath: .spec.virtualClusterRef
      name: VirtualCluster
      type: string
    - description: The internal IP of the pod
      jsonPath: .status.podIP
      name: Pod IP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualInstance is the Schema for the virtualinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualInstanceSpec định nghĩa cấu hình của một máy ảo
            properties:
              hostname:
                type: string
              image:
                type: string
              imagePullSecrets:
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              ports:
                items:
                  description: VirtualInstancePort định nghĩa các cổng mạng cần mở
                  properties:
                    expose:
                      type: boolean
                    name:
                      type: string
                    port:
                      format: int32
                      type: integer
                    targetPort:
                      format: int32
                      type: integer
                  required:
                  - expose
                  - name
                  - port
                  - targetPort
                  type: object
                type: array
              resources:
                description: VirtualInstanceResources gom nhóm tài nguyên tính toán
                  của VirtualInstance
                properties:
                  cpu:
                    description: VirtualInstanceResourceLimit định nghĩa CPU/Memory
                      request và limit
                    properties:
                      limit:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      request:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                  ephemeralStorage:
                    description: VirtualInstanceResourceLimit định nghĩa CPU/Memory
                      request và limit
                    properties:
                      limit:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      request:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                  memory:
                    description: VirtualInstanceResourceLimit định nghĩa CPU/Memory
                      request và limit
                    properties:
                      limit:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      request:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              storage:
                description: VirtualInstanceStorage gom cấu hình lưu trữ
                properties:
                  dataVolumes:
                    items:
                      description: VirtualInstanceDataVolume định nghĩa các ổ đĩa
                        mount thêm vào (như /var/lib/docker)
                      properties:
                        mountPath:
                          type: string
                        name:
                          type: string
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        type:
                          enum:
                          - local
                          - network
                          type: string
                      required:
                      - mountPath
                      - name
                      - size
                      - type
                      type: object
                    type: array
                  root:
                    description: VirtualInstanceRootVolume định nghĩa ổ đĩa chứa hệ
                      điều hành của Sysbox
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      type:
                        enum:
                        - local
                        - network
                        type: string
                    required:
                    - size
                    - type
                    type: object
                required:
                - root
                type: object
              virtualClusterRef:
                type: string
            required:
            - image
            - virtualClusterRef
            type: object
          status:
            description: VirtualInstanceStatus định nghĩa trạng thái quan sát được
              của VirtualInstance
            properties:
              accessEndpoints:
                description: Danh sách các link/endpoint cho UI hiển thị
                items:
                  description: VirtualInstanceAccessEndpoint chứa thông tin URL để
                    sinh viên bấm vào truy cập ngay
                  properties:
                    internalAddress:
                      type: string
                    name:
                      type: string
                    protocol:
                      type: string
                    url:
                      type: string
                  required:
                  - internalAddress
                  - name
                  - protocol
                  type: object
                type: array
              conditions:
                description: Chuẩn báo cáo điều kiện K8s
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                enum:
                - Pending
                - Creating
                - Running
                - Stopped
                - Failed
                type: string
              podIP:
                type: string
              podName:
                description: Tên Pod và IP thực tế dưới K8s
                type: string
              volumesStatus:
                description: Trạng thái đính kèm của các ổ đĩa
                properties:
                  dataVolumes:
                    items:
                      description: VirtualInstanceVolumeStatusDetail chứa trạng thái
                        của từng PVC
                      properties:
                        name:
                          type: string
                        pvcName:
                          type: string
                        status:
                          enum:
                          - Pending
                          - Bound
                          - Lost
                          - Failed
                          type: string
                      required:
                      - pvcName
                      - status
                      type: object
                    type: array
                  rootVolume:
                    description: VirtualInstanceVolumeStatusDetail chứa trạng thái
                      của từng PVC
                    properties:
                      name:
                        type: string
                      pvcName:
                        type: string
                      status:
                        enum:
                        - Pending
                        - Bound
                        - Lost
                        - Failed
                        type: string
                    required:
                    - pvcName
                    - status
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# ==============================================================================
# ROLE KUBEADM-MASTER: KHỞI TẠO CONTROL PLANE VÀ TRIỂN KHAI CALICO CNI
# ==============================================================================
- name: Check if Kubernetes control-plane is already initialized
  ansible.builtin.stat:
    path: /etc/kubernetes/admin.conf
  register: kubeadmin_conf_stat

- name: Render kubeadm init configuration file
  ansible.builtin.copy:
    dest: "{{ kubeadm_config_path }}"
    content: |
      apiVersion: kubeadm.k8s.io/v1beta4
      kind: InitConfiguration
      nodeRegistration:
        criSocket: unix:///run/containerd/containerd.sock
      ---
      apiVersion: kubeadm.k8s.io/v1beta4
      kind: ClusterConfiguration
      kubernetesVersion: {{ kubeadm_k8s_version | default('v1.35.0') }}
      networking:
        podSubnet: {{ kubeadm_pod_subnet | default('192.168.0.0/16') }}
      ---
      apiVersion: kubelet.config.k8s.io/v1beta1
      kind: KubeletConfiguration
      cgroupDriver: systemd
    mode: "0644"
  when: not kubeadmin_conf_stat.stat.exists

- name: Initialize Kubernetes cluster using kubeadm
  ansible.builtin.command:
    cmd: "kubeadm init --config {{ kubeadm_config_path }}"
  when: not kubeadmin_conf_stat.stat.exists
  changed_when: true

- name: Create .kube directory for root user
  ansible.builtin.file:
    path: /root/.kube
    state: directory
    mode: "0755"

- name: Copy admin.conf to root user kubeconfig
  ansible.builtin.copy:
    src: /etc/kubernetes/admin.conf
    dest: /root/.kube/config
    remote_src: true
    mode: "0600"

- name: Get home directory of ansible_user if not root
  ansible.builtin.user:
    name: "{{ ansible_user }}"
  register: ansible_user_info
  when: ansible_user is defined and ansible_user != 'root'

- name: Create .kube directory for ansible_user
  ansible.builtin.file:
    path: "{{ ansible_user_info.home }}/.kube"
    state: directory
    owner: "{{ ansible_user }}"
    mode: "0755"
  when: ansible_user is defined and ansible_user != 'root' and ansible_user_info.home is defined

- name: Copy admin.conf to ansible_user kubeconfig
  ansible.builtin.copy:
    src: /etc/kubernetes/admin.conf
    dest: "{{ ansible_user_info.home }}/.kube/config"
    remote_src: true
    owner: "{{ ansible_user }}"
    mode: "0600"
  when: ansible_user is defined and ansible_user != 'root' and ansible_user_info.home is defined

- name: Deploy Calico CNI networking plugin
  ansible.builtin.command:
    cmd: "kubectl apply -f {{ kubeadm_calico_url }}"
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  changed_when: true

- name: Taint master node with PreferNoSchedule to allow scheduling if needed
  ansible.builtin.command:
    cmd: "kubectl taint nodes --all node-role.kubernetes.io/control-plane:PreferNoSchedule --overwrite"
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  changed_when: false
  failed_when: false

- name: Generate worker node join command
  ansible.builtin.command:
    cmd: kubeadm token create --print-join-command
  register: join_cmd_raw
  changed_when: false

- name: Share worker join command across Ansible hosts
  ansible.builtin.set_fact:
    kubeadm_join_cmd: "{{ join_cmd_raw.stdout }}"
  delegate_to: "{{ item }}"
  delegate_facts: true
  loop: "{{ groups['k8s_workers'] | default([]) }}"
